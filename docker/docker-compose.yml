services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: stockrank:latest
    container_name: stockrank-api
    ports: ["8000:8000"]
    volumes:
      - ../artifacts:/app/artifacts
      - ../data:/app/data
    restart: unless-stopped

  dashboard:
    image: stockrank:latest
    container_name: stockrank-dashboard
    depends_on: [api]
    ports: ["8501:8501"]
    volumes:
      - ../artifacts:/app/artifacts
      - ../reports:/app/reports
    command: >
      streamlit run dashboard/app.py
      --server.port=8501 --server.address=0.0.0.0 --server.headless=true
    restart: unless-stopped

  research:
    image: stockrank:latest
    container_name: stockrank-research
    profiles: ["research"]
    volumes:
      - ../artifacts:/app/artifacts
      - ../data:/app/data
      - ../reports:/app/reports
    command: ["python", "-m", "stockrank.cli", "run", "--config", "configs/default.yaml"]
